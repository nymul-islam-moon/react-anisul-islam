import React, { Component } from "react";

export default class STATE extends Component {
    render() {
        return (
            <div>
                {this.props.count}
            </div>
        )
    }
}