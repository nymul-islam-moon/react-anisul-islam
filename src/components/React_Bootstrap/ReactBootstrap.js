import React from "react";

const ReactBootstrap = () => {
    return <div>
        <h1>Card Title</h1>
        <p>You need to install Node.js for local development. You can also choose to use Node.js in production, but you don’t have to. Many React frameworks support export to a static HTML/CSS/JS folder.</p>
        <button>Lorem More</button>
    </div>;
};

export default ReactBootstrap;